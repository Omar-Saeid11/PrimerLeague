<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrimerLeague" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PrimerLeague" parent="Base.Theme.PrimerLeague" />

    <style name="text" parent="Widget.MaterialComponents.TextView">
        <item name="android:textColor">@color/black</item>
        <item name="android:alpha">1</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:layout_marginBottom">@dimen/margin_24</item>
    </style>
    <style name="text.home">
        <item name="android:layout_marginTop">@dimen/margin_24</item>
        <item name="layout_constraintStart_toStartOf">@id/details</item>
        <item name="layout_constraintEnd_toStartOf">@id/matchStatistics</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>
    <style name="text.away">
        <item name="android:layout_marginTop">@dimen/margin_24</item>
        <item name="layout_constraintStart_toEndOf">@id/matchStatistics</item>
        <item name="layout_constraintEnd_toEndOf">@id/details</item>
    </style>
    <style name="text.statistics">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textColor">@color/white</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
    </style>
</resources>